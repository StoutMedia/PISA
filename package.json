{
  "name": "pisa-website",
  "version": "1.0.0",
  "private": true,
  "description": "A responsive static website for PISA.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "rm -rf dist",
    "serve": "python3 -m http.server 4173 --directory dist"
  }
}
